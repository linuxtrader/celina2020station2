#!/bin/bash

# RUN ONCE
# Run before git.setup
# Run git.setup before creating in netlify
# git.setup or this script, must add this to .gitignore

grep -q git.gitub .gitignore 2&> /dev/null || echo git.github >> .gitignore

build=celina2020station2
repo="${build}.dfwskyview.com"

curl -H "Authorization: token 11e30081d0b5419ce46ee5bd70e79d8f4575d904" --data '{"name":"'$repo'"}' https://api.github.com/user/repos | grep name
